import { Header, Text } from "@mantine/core";

export const Topbar: React.FC = () => {
  return (
    <Header height={60} styles={{
      main: {
        backgroundColor: "transparent",
      },
    }}>
      <Text> L </Text>
    </Header>
  );
};
